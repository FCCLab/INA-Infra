# central cluster initial export

Generated by `scripts/export-central-live.sh` from kubectl context `central@central`.

## MetalLB VIP pool

`namespaces/metallb-system/metallb-ipaddresspool.yaml`: **10.1.132.10–10.1.132.79**

| Service | VIP |
|---------|-----|
| Kubernetes Dashboard | **10.1.132.41** |

Mgmt dashboard uses **10.1.132.40** — central uses **.41** to avoid VIP conflict.

Push changes: `./scripts/push-central-to-gitea.sh`

Check sync: `./scripts/check-configsync.sh -c central@central -n central-repo`

Dashboard token: `./initial_central/get_dashboard_token.sh` (uses `KCTX=central@central`)
