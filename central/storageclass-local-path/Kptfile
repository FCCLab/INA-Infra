apiVersion: kpt.dev/v1
kind: Kptfile
metadata:
  name: storageclass-local-path
  annotations:
    config.kubernetes.io/local-config: "true"
info:
  description: Local-path provisioner and default StorageClass for single-node/lab clusters
