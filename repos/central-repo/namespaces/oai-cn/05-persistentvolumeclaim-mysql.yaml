kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: mysql
  namespace: oai-cn
  labels:
    app: mysql
    chart: mysql-8.0.31
    release: mysql
    heritage: Helm
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  storageClassName: local-path
