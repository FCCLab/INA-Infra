apiVersion: apps/v1
kind: Deployment
metadata:
  name: oai-cu-cp
  namespace: oai-slice-deployment
  labels:
    app.kubernetes.io/name: oai-cu-cp
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: oai-cu-cp
  template:
    metadata:
      labels:
        app: oai-cu-cp
        app.kubernetes.io/name: oai-cu-cp
      annotations:
        k8s.v1.cni.cncf.io/networks: '[{"name": "cucp-edge-e1", "interface": "e1",
          "ips": ["10.1.139.112/24"], "gateways": ["10.1.139.1"]}, {"name": "cucp-edge-f1c",
          "interface": "f1c", "ips": ["10.1.139.111/24"], "gateways": ["10.1.139.1"]},
          {"name": "cucp-edge-n2", "interface": "n2", "ips": ["10.1.139.110/24"],
          "gateways": ["10.1.139.1"]}]'
    spec:
      serviceAccountName: oai-cu-cp-sa
      terminationGracePeriodSeconds: 5
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
            - matchExpressions:
              - key: kubernetes.io/hostname
                operator: In
                values:
                - edge-0
                - edge-1
      containers:
      - name: cucp
        image: 10.1.132.30:5000/oai-cucp:nws-v0.8-amd64
        imagePullPolicy: IfNotPresent
        securityContext:
          privileged: true
        env:
        - name: TZ
          value: Europe/Paris
        - name: USE_ADDITIONAL_OPTIONS
          value: --log_config.global_log_options level,nocolor,time
        - name: USE_VOLUMED_CONF
          value: 'yes'
        ports:
        - name: n2
          containerPort: 36412
          protocol: SCTP
        - name: e1
          containerPort: 38462
          protocol: SCTP
        - name: f1c
          containerPort: 38472
          protocol: UDP
        volumeMounts:
        - name: configuration
          mountPath: /opt/oai-gnb/etc/gnb.conf
          subPath: gnb.conf
      - name: debug
        image: docker.io/nicolaka/netshoot
        command:
        - sleep
        - infinity
        securityContext:
          capabilities:
            add:
            - NET_ADMIN
            - NET_RAW
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 128Mi
      volumes:
      - name: configuration
        configMap:
          name: oai-cu-cp-configmap
