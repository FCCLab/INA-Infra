apiVersion: v1
kind: Service
metadata:
  name: dashboard-autologin
  namespace: kubernetes-dashboard
  labels:
    app.kubernetes.io/name: dashboard-autologin
spec:
  ports:
    - name: https
      port: 443
      protocol: TCP
      targetPort: https
  selector:
    app.kubernetes.io/name: dashboard-autologin
