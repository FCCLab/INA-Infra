# PRB benchmark results

Influx → numpy → plots for near-RT RIC PRB max% sweeps on `oai-benchmark`.

**5G / RF / traffic config**: see [`docs/oai-benchmark.md`](../../docs/oai-benchmark.md).

```bash
./data_download.py
./data_plot.py
```
