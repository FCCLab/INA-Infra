apiVersion: apps/v1
kind: Deployment
metadata:
  name: oai-amf-controller
  namespace: exp1-d
  labels:
    app.kubernetes.io/name: oai-amf
    app.kubernetes.io/part-of: ina-infra
    app.kubernetes.io/component: oai-controller
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: oai-amf
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: oai-amf
    spec:
      securityContext:
        runAsGroup: 0
        runAsUser: 0
      containers:
      - name: amf-controller
        image: docker.io/arorasagar/oai-amf-controller:v2.0.1
        imagePullPolicy: Always
        env:
        - name: NF_TYPE
          value: amf
        - name: DEPLOYMENT_FETCH_INTERVAL
          value: '1'
        - name: DEPLOYMENT_FETCH_ITERATIONS
          value: '100'
        - name: NF_CONF_PATH
          value: /tmp/nf/amf.yaml
        - name: OP_CONF_PATH
          value: /tmp/op/amf.yaml
        - name: TESTING
          value: 'no'
        - name: KUBERNETES_BASE_URL
          value: https://$(KUBERNETES_SERVICE_HOST):$(KUBERNETES_SERVICE_PORT)
        - name: DEBUG_SIDECAR
          value: 'yes'
        - name: DEBUG_SIDECAR_IMAGE
          value: docker.io/nicolaka/netshoot
        - name: SVC_TYPE
          value: ClusterIP
        volumeMounts:
        - mountPath: /tmp/op
          name: opconf
        - mountPath: /tmp/nf
          name: nfconf
        - name: utils-patch
          mountPath: /root/.local/utils.py
          subPath: utils.py
        resources:
          requests:
            memory: 256Mi
            cpu: 100m
          limits:
            memory: 256Mi
            cpu: 100m
        command:
        - /usr/local/bin/python
        - /root/.local/bin/kopf
        - run
        - /root/.local/controller.py
        - --namespace=ina-infra
      volumes:
      - configMap:
          name: oai-amf-op-conf
        name: opconf
      - configMap:
          name: oai-amf-nf-conf
        name: nfconf
      - name: utils-patch
        configMap:
          name: oai-amf-controller-utils
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      serviceAccountName: oai-amf-operator
      terminationGracePeriodSeconds: 5
      nodeSelector:
        kubernetes.io/arch: amd64
