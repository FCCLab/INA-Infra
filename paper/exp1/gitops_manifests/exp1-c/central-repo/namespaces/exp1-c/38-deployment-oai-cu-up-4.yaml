apiVersion: apps/v1
kind: Deployment
metadata:
  name: oai-cu-up-4
  namespace: exp1-c
  labels:
    app.kubernetes.io/name: oai-cu-up-4
    app.kubernetes.io/part-of: ina-infra
    slice: '4'
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: oai-cu-up-4
  template:
    metadata:
      labels:
        app: oai-cu-up-4
        app.kubernetes.io/name: oai-cu-up-4
        slice: '4'
      annotations:
        k8s.v1.cni.cncf.io/networks: '[{"name": "cuup-slice4-e1", "interface": "e1",
          "ips": ["10.1.140.84/24"], "gateways": ["10.1.140.1"]}, {"name": "cuup-slice4-f1u",
          "interface": "f1u", "ips": ["10.1.140.104/24"], "gateways": ["10.1.140.1"]},
          {"name": "cuup-slice4-n3", "interface": "n3", "ips": ["10.1.140.124/24"],
          "gateways": ["10.1.140.1"]}]'
        oai.nephio.org/upf-n3: 10.1.140.24
        oai.nephio.org/site: central
    spec:
      serviceAccountName: oai-cu-up-4-sa
      terminationGracePeriodSeconds: 5
      nodeSelector:
        kubernetes.io/arch: amd64
        ina-infra.nephio.lab/multus-master: enp7s0
      containers:
      - name: bringup-order
        image: docker.io/nicolaka/netshoot
        imagePullPolicy: IfNotPresent
        command:
        - bash
        - -c
        - 'set -eu

          echo "bringup-order sidecar role=cu-up-4"

          echo ''depends on (see exp1-c/bringup_order.md):''

          echo ''  - none (starts before CU-CP)''

          echo ''  - E1: dials CU-CP :38462 after CU-CP is up''

          echo ''gated by bringup-* initContainers; sleeping''

          sleep infinity'
        resources:
          requests:
            cpu: 5m
            memory: 16Mi
          limits:
            cpu: 50m
            memory: 64Mi
      - name: cuup
        image: 10.1.132.30:5000/oai-nr-cuup:nws-v0.8.2-amd64
        imagePullPolicy: IfNotPresent
        securityContext:
          privileged: true
        env:
        - name: TZ
          value: Europe/Paris
        - name: USE_ADDITIONAL_OPTIONS
          value: --log_config.global_log_options level,nocolor,time
        - name: USE_VOLUMED_CONF
          value: 'yes'
        ports:
        - name: n3
          containerPort: 2152
          protocol: UDP
        - name: e1
          containerPort: 38462
          protocol: SCTP
        readinessProbe:
          exec:
            command:
            - bash
            - -c
            - pgrep -f softmodem >/dev/null
          initialDelaySeconds: 15
          periodSeconds: 5
          timeoutSeconds: 2
          failureThreshold: 24
        volumeMounts:
        - name: configuration
          mountPath: /opt/oai-gnb/etc/gnb.conf
          subPath: gnb.conf
      - name: debug
        image: docker.io/nicolaka/netshoot
        command:
        - sleep
        - infinity
        securityContext:
          capabilities:
            add:
            - NET_ADMIN
            - NET_RAW
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 200m
            memory: 128Mi
      volumes:
      - name: configuration
        configMap:
          name: oai-cu-up-4-configmap
