apiVersion: v1
kind: Service
metadata:
  name: mysql
  namespace: exp4-s1
  labels:
    app: mysql
    app.kubernetes.io/part-of: ina-infra
    app.kubernetes.io/component: dedicated-core
spec:
  type: ClusterIP
  ports:
  - name: mysql
    port: 3306
    targetPort: mysql
  selector:
    app: mysql
